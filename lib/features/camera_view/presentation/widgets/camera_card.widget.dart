import 'package:flutter/material.dart';

class CameraCardWidget extends StatelessWidget {
  const CameraCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
